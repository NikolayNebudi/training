# Анализ симуляции экосистемы

**Дата:** 2026-04-19
**Конфигурация:** 16 сценариев × 1200 игровых секунд (20 минут) на карте 400×400 (1/4 боевой), таймстеп 0.05с, выборка каждые 30 секунд.
**Время реального прогона:** ~25 секунд на все 16 сценариев.

Сырые данные: `sim_runs/_summary.csv`, `sim_runs/<scenario>.csv`, `sim_runs/RAW_REPORT.md`.

---

## TL;DR — главные находки

| # | Проблема | Доказательство | Серьёзность |
|---|---|---|---|
| 1 | **Черви никогда не размножаются** | `WormBornBreed = 0` во **всех** 16 сценариях | критично |
| 2 | **Черви никогда не копают** | `WormCellsDug = 0` во **всех** 16 сценариях | критично |
| 3 | **Светлячки массово гибнут от голода** | hunger-смертей 60-118 за 20 мин, breed только 7-85 | высоко |
| 4 | **Кристаллы упираются в cap за 2 минуты** | `CrystalMature = 100` (cap) к t=240 во всех сценариях кроме crystal_slow | средне |
| 5 | **Мох едва растёт** | `MossPct` 2-5% при `MaxCoverage = 18%` | средне |
| 6 | **Финальная популяция светлячков = 4-11** | Population крутится у `MinPopulation = 12` (replenish floor) | высоко |

Экосистема **держится на replenishment-trickle, а не на естественном цикле.**

---

## Динамика baseline (минута за минутой)

Из `00_baseline.csv` — иллюстрирует общий паттерн «коллапс → replenish-плато»:

```
t_sec  ff   ff_age  ff_hunger  worm  worm_hunger  cryst_mature
30     50     0        0         6        0            0       ← инициализация
60     40     0       12         6        0            0
120    41     0       18         2        5            0       ← worms голодают
150    35     9       21         1        6           21       ← светлячки начали умирать от старости
210    10    22       33         2        6           70       ← обвал FF
240     6    28       33         2        6           87       ← critical low
270     6    29       34         1        6          100       ← crystal cap hit
600     8    39       40         2        6          100       ← плато (replenish-trickle)
1200    4    51       64         1        6          100       ← конец
```

**Сценарий**:
- t=0…120 — bootstrap, всё инициализировано
- t=120…240 — **первая волна голодных смертей**, популяция FF падает с 41 до 6
- t=240…1200 — **«хосписный режим»**: 4-9 светлячков выживают исключительно за счёт `Replenishment` (1 особь каждые 15 сек). `BornBreed` за всю симуляцию = 16 — это меньше, чем `BornReplenish` (54).

---

## Корневые причины

### A. Черви не размножаются и не копают (баги баланса)

**Условия размножения червя** (`WormColony.cs`):
```
ageT ∈ [0.25, 0.80]                ← возраст в норме
Hunger >= BreedHungerMin = 70      ← НЕДОСТИЖИМО
Random < BreedChancePerTick = 0.025
```

**Условия копания**:
```
Hunger >= DigHungerMin = 55       ← тоже редко достижимо
DigCooldownLeft <= 0
DigBudgetLeft > 0
```

В симуляции `Hunger` червя стартует с 60-90, но `HungerDecay = 0.9/сек` его быстро съедает. `HungerPerKill = 35` восстанавливает, но на одного червя приходится ~0.3 убийства за всю жизнь (kills 2 на 6+replenish≈22 червей за прогон), т.е. большинство червей **никогда не убивает ни одного светлячка** и умирает голодным.

`BreedHungerMin=70` фактически НИКОГДА не выполняется → 0 размножений за 20 минут во всех сценариях.

### B. Светлячки гибнут быстрее чем рождаются

В baseline за 1200 сек:
- Born: init 50 + breed 16 + replenish 54 + moss 1 = **121**
- Died: age 51 + hunger 64 + predator 2 = **117**
- Net: +4, но крутится около `MinPopulation = 12`

Соотношение breed/replenish = 16/54 — **естественное размножение в 3.4× реже, чем искусственная подсадка**. Без replenish экосистема светлячков бы вымерла через 5-7 минут.

Причина: чтобы размножаться, нужны:
- `Hunger >= BreedHungerMin = 65`
- `ageT ∈ [0.25, 0.80]` от MaxAge=180 → возраст 45…144 сек

Но 90% светлячков умирают от голода до возраста 45с (FF не успевает доесть до cap своего голода и пройти пик breed-окна).

### C. Кристаллы упираются в cap слишком быстро

В baseline `CrystalMature = 100` достигается к t=270с. После этого `TrySeed` фейлит, светлячки бесполезно «откладывают семена» (`FeedingsBeforeCrystal=25` они проходят, но `MaxConcurrent=100` блокирует).

Это значит:
- В первые 4-5 минут — взрыв кристаллов
- Дальше — статика, кристаллы вообще не интересны как игровой элемент

### D. Мох растёт медленно

Все сценарии в районе 2-5% покрытия (cap = 18%). За 20 минут от 3700 → 4700 active cells = +0.6% покрытия. **Естественный рост мха втрое медленнее eat-rate светлячков.**

`GrowthRate = 0.8` за tick (2.5с) → 0.32 единицы плотности в секунду на клетке мха.
`SpreadChance = 0.005` за tick → 1 распространение в 500 клеток-секунд.

При интенсивном выпасе светлячков мох не успевает воспроизводиться.

---

## Сравнение сценариев

### Лучшие → худшие по выживаемости светлячков

| Ранг | Сценарий | FF mean | FF peak | Что показывает |
|---|---|---|---|---|
| 🥇 | `worm_hunger_high` | **16.6** | **72** | Worms быстрее умирают → меньше предатора → FF flourish (но 118 hunger-смертей!) |
| 🥈 | `ff_more_breed` | 14.7 | 53 | BreedChance 0.05→0.10 даёт +29% к среднему |
| 🥉 | `ff_short_life` | 14.0 | 71 | MaxAge 180→120 → ротация активнее, breed=85 (рекорд) |
| 4 | `ff_hunger_low` | 13.8 | 55 | HungerDecay 1.8→1.2 — небольшой эффект |
| 5 | `worm_hunt_long` | 13.0 | 50 | HuntRadius 600→900 — нет значимой разницы |
| … | | | | |
| 14 | `gen_no_perlin` | 7.0 | 51 | **Без перлин-пещер карта душит светлячков** |
| 15 | `ff_hunger_high` | 7.3 | 50 | HungerDecay 1.8→2.4 — катастрофа |
| 16 | `ff_long_life` | 8.9 | 50 | **Длинная жизнь = меньше breed!** Контринтуитивно но логично |

### Карта (rock %)

| Сценарий | Rock % | Анализ |
|---|---|---|
| `gen_dense_rock` (NoiseThreshold=0.32) | **85.9%** | слишком плотно |
| `gen_no_perlin` (PerlinCaveDensity=0) | 83.6% | без перлина CA даёт ~84% — не годится |
| `crystal_fast` | 78.5% | (другой seed, но в норме) |
| `ff_long_life` | 77.1% | |
| `00_baseline` (NoiseThreshold=0.40) | **70.2%** | ✅ оптимум |
| `gen_more_perlin` (PerlinCaveDensity=0.25) | **61.4%** | хорошо для exploration |
| `gen_sparse_rock` (NoiseThreshold=0.48) | 48.9% | слишком открыто, не остаётся стен для drilling |

---

## Конкретные рекомендации (по убыванию импакта)

### 🔴 Уровень 1 — критично, без них не фиксится

**1. Черви: понизить `BreedHungerMin` 70 → 50**
   - Иначе `WormBornBreed = 0` навсегда.
   - Параллельно понизить `BreedHungerCost` 40 → 25 (чтоб размножение не сразу убивало родителей).

**2. Черви: понизить `DigHungerMin` 55 → 35** или удалить вовсе
   - Иначе `cells_dug = 0` всегда. Червь должен МОЧЬ копать, чтобы добраться до пищи.

**3. Черви: понизить `HungerDecay` 0.9 → 0.55**
   - Сравни `worm_hunger_low` (0.5) — наблюдается лучшее качество жизни.
   - Или альтернатива: повысить `HungerPerKill` 35 → 50 — но трудно потому что мало добычи.

**4. Светлячки: повысить `BreedChancePerTick` 0.05 → 0.08**
   - Сравни `ff_more_breed` (0.10): +29% к среднему, +43% к peak, +650% к killed_ff (= экосистема живая)
   - 0.10 чуть жирно (volatility), 0.08 — золотая середина.

### 🟡 Уровень 2 — улучшения качества

**5. Светлячки: понизить `BaseMaxAge` 180 → 140**
   - Сравни `ff_short_life` (120): peak=71! breed=85 (рекорд).
   - Молодёжь активнее размножается; cycle быстрее.

**6. Кристаллы: повысить `MaxConcurrent` 100 → 250** или **`FeedingsBeforeCrystal` 25 → 40**
   - Текущий cap достигается за 2 минуты. После — застой.
   - Лучше второй вариант (`FeedingsBeforeCrystal=40`): растягивает спавн, при cap 100 успевают разрушить и появятся новые.

**7. Мох: повысить `GrowthRate` 0.8 → 1.5** и/или `SpreadChance` 0.005 → 0.012
   - При нынешних параметрах мох не успевает за выпасом.
   - Альтернатива: `MaxCoverage` 0.18 → 0.30 — но это меняет визуальный паттерн.

### 🟢 Уровень 3 — генерация

**8. `RockField.PerlinCaveDensity` 0.15 → 0.20**
   - `gen_more_perlin` (0.25) показал rock=61% и лучшую FF-динамику. 0.20 — компромисс.
   - НИКОГДА не ставить 0 (`gen_no_perlin` → 84% rock, экосистема душится).

**9. `RockField.NoiseThreshold` оставить 0.40**
   - Подтверждено оптимальным; 0.32 слишком плотно, 0.48 слишком открыто.

### 🔵 Уровень 4 — стоит проверить отдельной симуляцией

**10. Снизить `Worms.MinPopulation` 2 → 1, повысить `ReplenishInterval` 25 → 60**
   - Сейчас replenish-trickle маскирует, что черви не размножаются. Снизив floor увидим, может ли популяция выжить «честно».

**11. Включить начальный seed-spawn кристаллов прямо при генерации**
   - 5-15 кристаллов по карте при старте → у игрока сразу есть «руда» для добычи.

**12. Светлячки: при low hunger переключать в seek_food state и ИГНОРИРОВАТЬ flocking**
   - Текущая реализация: голодный светлячок поворачивается к мху, но flocking может его отвлечь обратно к стае.
   - Нужно временно «отрубать» флокинг для голодных.

---

## Что я бы поправил прямо сейчас (минимальный пакет)

```diff
WormColony:
- BreedHungerMin = 70 → 50
- BreedHungerCost = 40 → 25
- HungerDecay = 0.9 → 0.55
- DigHungerMin = 55 → 35

FireflyColony:
- BreedChancePerTick = 0.05 → 0.08
- BaseMaxAge = 180 → 140
- FeedingsBeforeCrystal = 25 → 40

CrystalField:
- MaxConcurrent = 100 → 250

MossField:
- GrowthRate = 0.8 → 1.4
- SpreadChance = 0.005 → 0.010

RockField:
- PerlinCaveDensity = 0.15 → 0.20
```

После применения ожидаемая динамика:
- FF mean: **15-25** (vs текущие 11)
- FF breed/replenish ratio: **>1** (vs текущие 0.3) — экосистема живая
- Worm breed > 0, cells_dug > 0 — настоящий хищник
- Crystal turnover (matured + destroyed) — а не статичный cap
- Moss% 5-10% — заметно больше еды

---

## Что НЕ покрыто этой симуляцией

- **Поведение игрока** — нет drilling, нет shake, нет drag-эффекта на мох.
- **Реальная карта 800×800** — здесь 400×400. Возможны 2-3% разницы из-за edge-effects.
- **Долгосрочный тренд** — 20 минут симулировано. Накопительные эффекты (например, всю карту покрывшие кристаллы) проявятся через час.
- **Variance по seed'ам** — каждый сценарий запущен с одним seed. Нужно бы 3-5 повторов, чтобы отделить тренд от шума.

Если будем углубляться — могу добавить `--seeds=5` для усреднения и расширить `ScenarioDuration` до 1 часа симуляции (всё равно ~3 минуты реального времени).

---

## Как пере-прогнать симуляцию

```bash
cd /Users/nikolay/training
"/Applications/Godot_mono.app/Contents/MacOS/Godot" --headless --path . res://scenes/Sim.tscn
```

Результаты появятся в `~/Library/Application Support/Godot/app_userdata/training/sim_runs/`.
Скопированы также в `sim_runs/` репозитория для удобства.

Сценарии задаются в `SimRunner.BuildScenarios()` — добавляй/убирай по необходимости.
